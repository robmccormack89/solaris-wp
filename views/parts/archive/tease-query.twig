<!-- <div class="uk-grid">
  
  <div class="uk-width-auto">
    <img data-src="{{ post.thumbnail.src('solaris-theme-tiny-thumb') }}" height="80" width="80" alt="" uk-img>
  </div>
  
  <div class="uk-width-expand">
    <a title="{{post.title}}" href="{{post.link}}">{{post.title}}</a>
  </div>
  
</div> -->
<div class="uk-card uk-card-body" style="padding:10px 0;">
<div class="uk-flex-middle uk-margin-remove-top uk-grid-small" uk-grid>
    <div class="uk-width-expand">
      <a class="" title="{{post.title}}" href="{{post.link}}">{{post.title}}</a>
      <p class="uk-text-meta uk-margin-remove-top">{{ post.preview.length(6).read_more('') }}</p>
    </div>
    <div class="uk-width-1-4 uk-flex-first">
        <img data-src="{{ post.thumbnail.src('solaris-theme-more-stories-image') }}" alt="" uk-img>
    </div>
</div>
</div>